server:
    port: 8972

spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:6033/history_db?useUnicode=true
    username: history
    password: history
  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: false
    show-sql: true
mybatis:
  mapper-locations: classpath:mapper/**/*.xml
