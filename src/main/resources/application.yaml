server:
    port: 8972

spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:6033/sample?useUnicode=true
    username: todo_user
    password: todo_passwd

mybatis:
  mapper-locations: mapper/**/*.xml