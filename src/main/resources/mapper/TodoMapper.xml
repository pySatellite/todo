<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="canary.rest_db.repository.TodoMapper">
    <select id="findAll"
            resultType="canary.rest_db.dto.Todo">
        SELECT 
            id, subject, body, completed
        FROM todo
        ORDER BY id DESC
    </select>

    <insert id="createTable">
        CREATE TABLE IF NOT EXISTS todo (
            id 	INT NOT NULL AUTO_INCREMENT,
            subject VARCHAR(50),
            body VARCHAR(50),
            completed TINYINT,
            PRIMARY KEY(id)
        )
    </insert>

    <insert id="insert" parameterType="canary.rest_db.dto.Todo">
        INSERT INTO todo(subject, body, completed)
        VALUES (
            #{subject},
            #{body},
            #{completed}
        )
    </insert>
</mapper>
